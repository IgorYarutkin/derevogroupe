mixin gallery(data)
	+b.gallery&attributes(attributes)
		each container in data
			+e.item
				each image in container
					+e.item-inner
						+e.image(src=image.file width=image.width height=image.height)
						if image.popup
							+e.popup
								+e.popup-title= image.popup.title
								+e.popup-text= image.popup.text
		+e.button Посмотреть далее
